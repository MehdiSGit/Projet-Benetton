{% extends 'base.html.twig' %}

{% block title %}Recruteur{% endblock %}

{% block body %}
    <h1>Recruteur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ recruteur.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ recruteur.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ recruteur.roles ? recruteur.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ recruteur.password }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ recruteur.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ recruteur.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('crud_recruteur_index') }}">back to list</a>

    <a href="{{ path('crud_recruteur_edit', {'id': recruteur.id}) }}">edit</a>

    {{ include('crud_recruteur/_delete_form.html.twig') }}
{% endblock %}
